import { createContext } from "react";



export const myContext = createContext();

//The createContext function is used to create a new context object, which is then exported as myContext for other components to access and use the context value.